{
  "name": "bgzf-wasm-build",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:wasm": "bash build-wasm.sh",
    "build:bundle": "webpack --config webpack.config.js",
    "build": "pnpm build:wasm && pnpm build:bundle"
  },
  "devDependencies": {
    "webpack": "^5.0.0",
    "webpack-cli": "^5.0.0"
  }
}
